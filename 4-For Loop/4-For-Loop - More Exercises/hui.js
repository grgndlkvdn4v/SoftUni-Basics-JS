function equalPairs(params) {
    index = 0;

    const totalPairs = Number(input[index]); //0
    index++;
    
    let sum = 0;
    let maxdiff = 0;
    let diff = 0;

    let minSum = 0;
    let maxSum = 0;

    for (let i = 1; i <= totalPairs; i++) {
        
        const numOne = Number(input[index]); //1
        index++;
        
        const numTwo = Number(input[index]); //2
        index++;

        sum = numOne + numTwo;
        
}


}
equalPairs(["2",   "1", "2",   "2", "2"]);
